import { lusitana } from "@/app/ui/fonts";

export default function PhonoQuestLogo() {
  return (
    <div className="flex items-center">
      <p className={`${lusitana.className} text-xl font-bold`}>PhonoQuest</p>
    </div>
  );
}
